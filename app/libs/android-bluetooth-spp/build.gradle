apply plugin: 'com.android.model.library'

model {
    android {
        compileSdkVersion 24
        buildToolsVersion "24"

        defaultConfig {
            versionCode 1
            versionName "1.0.0"
        }
    }
}